--- checkIfOutside funktion in check dead gesetzt
dadurch kann checkIfOutside aus RiverParticle rausgenommen werden

--- canals 
- funktion umbeanannt 
- unnötige sachen aus draw in wasserbau entfernt
- if abfrage für oversorting distance

--- testnoise
- auch mit neuer setAnglefunktion
- int numberOfTestParticles = int(m_size.x/2);
- testNoiseParticles[i].p.set(float(i+2), 0.0);


--- erstellen von neuen partikeln nach zeit
- erstellen von river particles in setup raus


--- noise, mit ofVec Shift verschieben

______

- "mulitoctave perlin noise"

------


- setAngleWithPerlinNoise anders 
- cases für flussbreite
- int in particel für noiseStrenght + noiseMedianShift

   - jeder partikel ein vector mit zwei werten 
      oder einfach zwei floats
   - dieser bezeichnet die noise strength und den entsprechenden 
   shiftMedian mit der er losgeflogen ist

noise:
float levelsOfNoiseStrength[4] = {8, 7, 6, 4};
float noiseMedianOffsetValues[4] = {0.5, -1, -0.5, 1};
float fittingNoiseMedianOffset[4];

for()

- cases in erstellen der partikel
- noiseParitkel auf 8 strenght
